# Portfolio

This repository contains my works.

## Getting Started

- Clone the repository
- Install its dependencies using npm:

```bash
$ npm install
```

- Run the development server locally with the following command:

```bash
$ npm run dev
```

- Alternatively, build the server and run with:

```bash
$ npm run build
$ npm run deploy
$ npm run preview
```

- This will start the server on `http://localhost:5173`.

## Develop your application

Once the command run you can view the portfolio website.

this is hosting provider in netlify. : https://monika-patel-portfolio.netlify.app/
